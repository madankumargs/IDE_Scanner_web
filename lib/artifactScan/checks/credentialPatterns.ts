import type { Finding, SourceFile } from "../types"; import { finding, matches } from "./helpers";
export function checkCredentialPatterns(file: SourceFile): Finding[] { const re = /sk-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|(?:key|secret|token|password|credential)\s*[:=]\s*['"][A-Za-z0-9_-]{32,}['"]/gi; return matches(file.content, re).map(m => finding(file, "credential_pattern", "Hardcoded credential pattern", "A string resembles a hardcoded API key or secret.", "Remove the credential, rotate it immediately if real, and load secrets through a protected runtime secret store.", "high", m.index!)); }
